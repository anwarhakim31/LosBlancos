import styles from "./format.module.scss";

const ImageFormat = () => {
  return (
    <div className={styles.info}>
      <p>
        <span>Format File :</span> PNG, JPG, JPEG, WEBP.
      </p>
    </div>
  );
};

export default ImageFormat;
